# 优化版说明

此目录是在原项目基础上创建的独立优化副本：

- 原项目：`G:\ZephyrWhispe\comic-downloader-integrated`
- 优化版：`G:\ZephyrWhispe\comic-downloader-integrated-optimized`

## 本次优化重点

- 修复 GetComics GUI 下载时的路径类型问题
- 修复重定向下载后文件可能落到错误目录的问题
- 让 GUI 和系列批量下载不再被终端重命名提示卡住
- 修复系列搜索多页时反复只查第一页的问题
- 让 CLI 菜单里的 `date / min / max / results` 筛选真正作用到 GetComics 搜索结果
- 修复 Comic-DL 在 `readallcomics.com`、`readcomicsonline.ru`、`xoxocomic.com` 上的目录/图片提取问题
- 新增 `sites/` 站点注册层，把每个网站的接入配置拆开，后续新增网站时不需要再改主下载流程或 GUI 里的域名硬编码
- 优化 GetComics 抓取逻辑：
  - 查询参数编码
  - 结果数量限制
  - 先查缓存再请求
  - 复用 `aiohttp` 会话
- 修复 GUI 结果列表与实际下载链接映射不一致的问题
- 增强取消下载时的响应
- 回收 Playwright `context` / parser 资源，减少长时间运行时的资源占用
- 为 `run.bat` / `run-gui.bat` 增加缺依赖提示
- 为 AI 重命名增加更明确的接口错误提示

## 已完成验证

- 已在优化版目录创建 `venv`
- 已安装 `requirements.txt` 依赖
- 已执行 `playwright install chromium`
- 已通过以下导入/编译检查：
  - `core.gui`
  - `core.comic_downloader`
  - `core.getinfo`
  - `core.series_downloader`
  - `python -m compileall core parsers`
  - `python -m unittest discover -s tests -v`

## 启动方式

- GUI：运行 `run-gui.bat`
- CLI：运行 `run.bat`

## 说明

- AI 重命名仍兼容当前内置 DeepSeek key，同时支持优先读取环境变量 `DEEPSEEK_API_KEY`
- 原项目未被修改，可继续保留作对照或回退使用
